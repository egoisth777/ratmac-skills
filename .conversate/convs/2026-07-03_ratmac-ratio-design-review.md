+++
id = "conv_260703_ratmac-ratio-design-review"
topic = "ratmac-ratio design review (superpowers / grill-with-docs lenses) and applied fixes"
status = "active"
tags = ["design-review", "grilling", "ratmac-ratio", "ratmac-skills", "skill-design"]
refs = [
  { id = "conv_260702_ratmac-ratio-design-and-automata-boundary-grilling", rel = "informed-by" },
]
created = 2026-07-03T20:32:22Z
updated = 2026-07-04T09:05:42Z
+++
## summary
External-review pass on the ratmac-ratio SPEC/DESIGN grilling skill (still in DESIGN), read through the obra/superpowers skill-writing lens and Matt Pocock's grill-with-docs technique against the ratmac-skills family R-invariants. Verdict: the core loop is strong (exact sign-off gates, lint-gated sign-off, domain-labelled one-question grilling, linted glossary) but the RV-2 de-mirror was only half-executed. Seven accepted fixes were applied the same day; RV-4 and several open RVs remain to resolve before DESIGN sign-off.

Discussion continued into the full-stack pipe vision (SEED WHY -> SPEC/DESIGN WHAT+HOW -> TEST -> ticket implementation; gate-grammar framing; stage 4 = connect gen-1 task family via derivation + evidence edges). Focus narrowed to first half (creavit+ratio). User requested debt resolution by grilling ("resolve these debts one by one by you asking me the questions") — main session now operates AS ratmac-ratio under the contract: read-gate satisfied via full section reads (spec § Seed input boundary / Grilling / Artifact workflow; design § Implementation Decisions 4 subsections / Technical Constraints; review.md protocol + RV-3..14 all open, statuses verbatim). Q1 asked: adopt escalation predicate (agent decides reversible DESIGN-only choices, escalates one-way/SPEC-affecting) now vs at publish vs tighter scope.

Grilling session executed decisions #60-#68. #60 escalation predicate adopted now (user); #61 linter = skill-local script (agent-decided); #62-#66 RV-3/5/11/12/14 accepted + executed (agent-decided; #33 annotation deferred to RV-1). Adversarial wave: Traceability Auditor CLEAN; SPEC Guardian + DESIGN Skeptic independently converged on an RV-11-induced role collapse ("a downstream owner" both never-mutates and writes phase=TEST) → repaired as #67 (roles renamed: "a downstream orchestration owner" vs "the dispatched TEST-owner skill") + spec Answer-field widened. User corrected two main-session premise errors (schema-tier promotion idea; "issues get archived" premise) → actual lifecycle recorded as #68: issue folder is contract home for the issue's life; future goal/ integration (unbuilt, downstream) integrates signed-off design into goal/ and retires the issue; publish regenerates shells from the issue folder. Main-session conduct tightened: every question premise now carries a source citation or an explicit [assumption] tag; questions in prose, plain language after two widget disorientations.

User decided the writer model (recorded as decision #69): one persistent writer per session, queued assignments, no coordination apparatus, completions surface as concise non-interrupting notifications; audit's writer-machinery finding resolved as amended; AGENTS.md TEMP block removed via restore-to-HEAD (sunset clause met). Global CLAUDE.md gained a "Code-names in chat" rule (plain-words summary first, bare IDs never). Three grilling frameworks studied via subagents from primary sources: grill-with-docs (recommendation attached to every question, dependency ordering, inline glossary/ADR with three-gate one-way test), superpowers brainstorming (scope gate before questions, multiple-choice preferred, 2-3 approaches with recommendation, section-by-section design approval, spec self-review + user review gate), compound engineering (rigor-gap probes over a grounding dossier, depth tiers, requirements-only contract, docs/solutions compounding loop read at plan+review time, ask-only-on-material-forks). Synthesis delivered: ratio already matches/beats on gates/ledger/glossary/WHAT-HOW/escalation; adoption candidates grouped as (1) question conduct, (2) convergence mechanics, (3) compounding loop (family-level, spin-off). Research note written to ratmac-sp/research/grilling-frameworks.md (may still be in flight).

Session continued (the user switched to Opus 4.8 1M mid-session). Decisions #70 (three grilling behavior groups folded into SPEC) and #71 (three review lenses plus a consolidating executor) were recorded. A third review wave then ran under the new consolidator model and worked: three lenses routed to one consolidator, which found two one-line vestigial writer-fleet leftovers from #69 (a dead "edit collision" pause-trigger in spec.md at two sites, and a self-contradicting status-ledger sentence in design.md) — being scrubbed as #73. The user accepted a SEED intake gate (recorded #72): a SEED must state WHY plus the core behavior group plus the scope boundary, must NOT contain requirement-grade WHAT or design-grade HOW, is intake-checked at grilling start, and a failing SEED is returned upstream with its gaps named; the SPEC Driving Question derives from the SEED WHY. The user then raised the key open design question: the upstream return HANDOFF CONTRACT (ratio → creavit) — how the handoff works and what the contract is — and wants it detailed. The grilling question was posed with three options (symmetric record-and-stop [recommended] / dependency-spark upstream / split-by-trigger), and four parts were asserted as already-determined: ratio never edits the SEED; ratio never flips phase (creavit writes phase=SEED on reactivation, per #57/#67); gaps ride in one DECISION entry with no separate artifact; same-project, so never IPC.

Session then pivoted to designing ratio's OWN grilling flow (how ratio conducts a SPEC/DESIGN interview), and banked the backbone as decision #75: an ARTIFACTUAL spine - the contract's own required sections ARE the coverage checkpoints, questions walk them in dependency order, and grill-toward equals validate-against (one source of truth) - COMBINED with a cross-session LESSON STORE (compound-engineering, now in scope). Factual questions self-answer from source; canonical terms flow to the glossary and hard-to-reverse calls to decision records the moment they resolve. This fuses grill-with-docs and compound-engineering (chosen over superpowers' procedural spine; grounded in ratmac-sp/research/grilling-frameworks.md). A per-turn SPEC-stage loop was drafted (still in grilling, not finalized): orient -> self-answer -> locate the first unsatisfied required section in dependency order -> ask ONE question with a recommended answer -> integrate (draft section + glossary + decision record) -> advance-or-stay -> close (lint -> offer "SPEC signed off" -> write lessons back). The DESIGN stage runs the same loop against the DESIGN contract's sections, plus ratio self-decides reversible DESIGN-only choices and escalates only material forks - recognized as the SAME mechanic as compound-engineering's ask-only-on-material-forks, giving one escalation predicate across both stages. The user then issued three corrections: (1) the SEED IS the SPEC's original draft (the SPEC stage opens it as the initial draft and optimizes/extends it, not a separate input); (2) DESIGN is cross-domain and may reach back to edit SPEC-domain artifacts (spec.md), not only design.md; (3) a CORE REQUIREMENT that spec.md (WHAT only) and design.md (HOW only) stay strictly domain-separated with no content bleed, joined only by the single existing cross-link.

The grilling then banked decisions #76-#80, resolving several open forks and opening one new one. #76 formalized the three corrections (the SEED is the SPEC's original draft; DESIGN is cross-domain and may edit spec.md in place; the core WHAT-only / HOW-only separation with a single interlink). A read-only separation audit found REAL HOW-bleed in spec.md (graph/registry/message-mechanism detail at three sites) that overlaps already-accepted-but-unexecuted cuts (the dependency-graph migration to automata and the message/IPC removal); remediation is DEFERRED pending a user go-ahead, and one borderline (design.md's scope section duplicating spec.md ownership) took a provisional agent-decided call — ownership stays canonical in spec.md and design.md links to it. #77 set the amendment model: DESIGN-driven SPEC edits are recorded as scoped amendments with NO per-edit re-sign, and a single full-scale cross-domain reconciliation at DESIGN close re-ratifies the amended SPEC and IS the home of the three-lens adversarial review plus consolidator (this closes the earlier open question about what happens to a signed-off spec.md when DESIGN back-edits it). #78 fixed the advance criterion: "section satisfied" is mechanical coverage only, fully frictionless with no mid-stage confirmation even on material forks, deferring all holistic approval to the end gate; the per-turn loop is finalized as orient -> self-answer -> locate the first unsatisfied required section (dependency order) -> ask ONE recommended question -> integrate (draft + glossary + decision record) -> advance-on-coverage -> close (lint -> offer sign-off -> write lessons). #79 chose the glossary home: ONE shared project-wide ubiquitous-language file at .arca/ubi-lang.md governing both the scheduler and the knowledge base, which ratio writes and consumes but does NOT own, lazily created, with a carve-out added to .arca/AGENTS.md — and a path fact was pinned: .arca/ is a real LOCAL repo directory (only its scheduler/ and space/ subpaths symlink into brain), so ubi-lang.md is a LOCAL file at .../ratmac-skills/.arca/ubi-lang.md, not brain. #80 set the gate shape: the sign-off / reconciliation gate is a TWO-PASS FAIL-FAST (Pass 1 a deterministic script prefilter, Pass 2 the three-lens subagent-team review), while the per-section advance-linter stays a fast script plus advisory separation heuristics; a scope assumption was flagged that the subagent team runs at the gate only, not per advance. The scheduler DECISION.md is now at 80 entries, all banked by a persistent artifact-writer subagent into the issue (i-ratmac-ratio); nothing is committed in git. The active grill question is now the lesson-store REUSE BOUNDARY (per-project / per-skill-kind [main-agent lean] / global); the lesson-store SCHEMA is the next grill after it, and separation remediation, the RV simplification cuts, and a deferred adversarial-review pass remain pending.

## dict
- **ratio / ratmac-ratio** - the SPEC/DESIGN grilling skill currently under design (the 12th skill, sitting in DESIGN under the issue tier).
- **shells** - skills/ratmac-ratio/SKILL.md + REFERENCE.md; deliberately rule-free during DESIGN, regenerated from the contract at publish as GENERATED-fenced views.
- **contract docs** - .arca/scheduler/issue/i-ratmac-ratio/{spec.md,design.md,DECISION.md}; the sole statement of rules while phase = DESIGN (resolve via mount to E:/repos/brain/scheduler/p-ratmac-skill/issue/i-ratmac-ratio/).
- **de-mirror / RV-2** - the review.md finding that killed rule duplication across the shells and the contract; executed in this pass.
- **ponytail audit** - review.md: 14 ranked over-engineering findings (RV-1 accepted-not-executed, RV-2 executed, RV-3..RV-14 open).
- **publish** - the step at DESIGN sign-off that deterministically and idempotently regenerates the shells from the contract as GENERATED-fenced views; the contract stays source of truth (R6/R10 aligned).
- **grilling** - a one-question-at-a-time interview with domain labels (SPEC-only | DESIGN-only | combination | hybrid); now carries a self-answer-before-asking rule.
- **sign-off phrases** - the exact strings "SPEC signed off" / "DESIGN signed off" (plus ": <issue-id>" variants); informal approval never counts, paraphrase is not sign-off, the phrase is never solicited, and a lint failure refuses it.
- **gen-2 family** - ratmac-creavit / ianitor / inqui / automata / int; newer lifecycle skills the contract references but that are absent from the committed README family; gen-2 names are pdrft-brain-v3 pending.
- **baseline** - the user's drift-correction hook system; design.md carries a stage-baseline renderer that RV-7 wants cut.
- **R-invariants (R3 / R6 / R10)** - README family invariants; R3 re-locks at publish, and R6/R10 align on the contract-as-source-of-truth plus generated-shells model.

## qa
- **Q:** Who writes phase=TEST (the old DECISION #46 vs #57 tension)? **A:** Closed by #67 - the RV-11 edit had collapsed one "downstream owner" role into both never-mutating and writing phase=TEST; the roles were split into "a downstream orchestration owner" (never mutates) vs "the dispatched TEST-owner skill" (writes phase=TEST), with the spec Answer-field widened. RV-14 hygiene executed (#62-#66).
- **Q:** Where do the contract docs durably live after i-ratmac-ratio? **A:** Closed by #68 - the issue folder is the contract's home for the issue's whole life; the (unbuilt, downstream) goal/ integration will fold the signed-off design into goal/ and retire the issue, and publish regenerates the shells from the issue folder. The earlier "issues get archived" and schema-tier-promotion premises were user-corrected.
- **Q:** RV-4 - inline vs one async writer vs a writer fleet (the AGENTS.md TEMP block) for SPEC-affecting-answer writing? **A:** Closed by #69 - one persistent writer per session, queued assignments, no coordination apparatus, completions surface as concise non-interrupting notifications; the audit's writer-machinery finding is resolved as amended, and the AGENTS.md TEMP block was removed via restore-to-HEAD (the sunset clause was met).
- **Q:** The grilling-framework adoption cut - which studied groups fold into the contract now? **A:** Closed by #70 (three grilling behavior groups folded into SPEC) and #71 (three review lenses plus a consolidating executor); extended by #75, which pulls the compounding loop's cross-session lesson store into scope now. Framework choice: grill-with-docs + compound-engineering over superpowers' procedural spine.
- **Q:** What is ratio's grilling backbone (the through-line for how it conducts a SPEC/DESIGN interview)? **A:** Closed by #75 - an ARTIFACTUAL spine: the contract's own required sections ARE the coverage checkpoints, questions walk them in dependency order, and grill-toward equals validate-against (one source of truth) - COMBINED with a cross-session LESSON STORE (compound-engineering). Factual questions self-answer from source; canonical terms flow to the glossary and hard-to-reverse calls to decision records the moment they resolve. Grounded in ratmac-sp/research/grilling-frameworks.md.
- **Q:** How do the SEED, spec.md, and design.md relate (three user corrections)? **A:** Banked as #76 - (1) the SEED IS the SPEC's original draft, which the SPEC stage opens as the initial draft and optimizes/extends, not a separate input; (2) DESIGN is cross-domain and may reach back to edit SPEC-domain artifacts (spec.md) in place, not only design.md; (3) CORE REQUIREMENT - spec.md (WHAT only) and design.md (HOW only) stay strictly domain-separated with no content bleed, joined only by the single existing cross-link.
- **Q:** When a DESIGN decision forces a WHAT change, ratio must edit spec.md in place, but spec.md was already signed off to enter DESIGN - what happens to that sign-off? **A:** Closed by #77 (amendment model) - DESIGN-driven SPEC edits are recorded as scoped amendments with NO per-edit re-sign; a single full-scale cross-domain reconciliation at DESIGN close re-ratifies the amended SPEC and is the home of the three-lens adversarial review plus consolidator. (Chosen over full re-sign and over a per-edit amendment sub-gate.)
- **Q:** The per-turn loop's "section satisfied" advance criterion - coverage-only, or confirm on material forks, or per-section approval? **A:** Closed by #78 - mechanical coverage only, fully frictionless (no mid-stage confirmation even on material forks); all holistic approval defers to the end gate. The per-turn loop is finalized: orient -> self-answer -> locate the first unsatisfied required section (dependency order) -> ask ONE question with a recommended answer -> integrate (draft + glossary + decision record) -> advance-on-coverage -> close (lint -> offer sign-off -> write lessons).
- **Q:** Where does the shared ubiquitous-language glossary live and who owns it? **A:** Closed by #79 - ONE project-wide file at .arca/ubi-lang.md governing both the scheduler and the knowledge base; ratio writes and consumes it but does NOT own it; it is lazily created, and a carve-out was added to .arca/AGENTS.md. PATH FACT: .arca/ is a real LOCAL repo directory (only its scheduler/ and space/ subpaths symlink into brain), so ubi-lang.md is a LOCAL file at .../ratmac-skills/.arca/ubi-lang.md, not brain.
- **Q:** What is the shape of the sign-off / reconciliation gate and the per-section advance-linter? **A:** Closed by #80 - the sign-off / reconciliation gate is a TWO-PASS FAIL-FAST (Pass 1 a deterministic script prefilter, Pass 2 the three-lens subagent-team review); the per-section advance-linter stays a fast script plus advisory separation heuristics. Flagged scope assumption: the subagent team runs at the gate only, not per advance.
- **Q (open):** CURRENT active grill question - the lesson-store REUSE BOUNDARY: (A) per-project, (B) per-skill-kind [main-agent lean], (C) global.
- **Q (open):** PENDING - separation remediation: execute the spec.md HOW-bleed cleanup (graph/registry/message-mechanism detail at three sites), which overlaps the already-accepted-but-unexecuted cuts (dependency-graph migration to automata; message/IPC removal). Deferred awaiting a user go-ahead.
- **Q (open):** PENDING - the lesson-store SCHEMA (the fields/shape of a stored lesson); the next grill after the reuse boundary resolves.
- **Q (open):** PARKED - the upstream return handoff contract (ratio -> creavit): symmetric record-and-stop (recommended) | dependency-spark upstream | split-by-trigger. Already-determined: ratio never edits the SEED; ratio never flips phase (creavit writes phase=SEED on reactivation, per #57/#67); gaps ride in one DECISION entry with no separate artifact; same-project, so never IPC.
- **Q (open):** PARKED - follow-on forks down the handoff branch once its shape is chosen: partial-SPEC handling on a mid-SPEC rewind (preserve+revalidate vs discard); status blocked-vs-active during the return; intake-failure vs mid-SPEC-rewind, whether that is one contract or two.
- **Q (open):** PARKED - remaining RV queue: single-issue simplification (RV-8); cross-project messaging, keep the seam and cut the transport (RV-6); baseline renderer cut (RV-7); link-reciprocity + lint-list guarantees (RV-9/RV-13); graph/registry migration target (RV-1, the i-ratmac-automata folder is still absent); grilling-economics leftovers; a deferred adversarial-review pass.
- **Q (open):** Shells (SKILL.md / REFERENCE.md) still carry stale writer-fleet wording - parked for publish-regen.
- **Q (open):** README long-term - amend the gen-1 invariants in place, or mark them superseded by pdrft-brain-v3 at ratio publish?

## resume
Mid-grilling on i-ratmac-ratio DESIGN, designing ratio's OWN SPEC/DESIGN grilling flow. The backbone (#75) is set: an artifactual spine (the contract's required sections ARE the coverage checkpoints, walked in dependency order, grill-toward = validate-against) plus a cross-session lesson store; factual questions self-answer from source, canonical terms go to the glossary and hard-to-reverse calls to decision records the moment they resolve (grill-with-docs + compound-engineering, over superpowers' procedural spine). Decisions #76-#80 are banked: #76 the three corrections (the SEED IS the SPEC's original draft; DESIGN edits cross-domain into spec.md in place; spec.md WHAT-only / design.md HOW-only, single interlink); #77 the amendment model (DESIGN-driven SPEC edits are recorded scoped amendments with NO per-edit re-sign; one full-scale cross-domain reconciliation at DESIGN close re-ratifies the amended SPEC and hosts the three-lens adversarial review + consolidator); #78 the advance criterion (section satisfied = mechanical coverage only, fully frictionless, no mid-stage confirm; the per-turn loop finalized as orient -> self-answer -> locate first unsatisfied section -> ask one recommended question -> integrate draft+glossary+decision-record -> advance-on-coverage -> close [lint -> offer sign-off -> write lessons]); #79 the glossary home (ONE project-wide .arca/ubi-lang.md governing scheduler + knowledge base, ratio writes/consumes but does not own, lazy-created, carve-out added to .arca/AGENTS.md; PATH FACT: .arca/ is a LOCAL repo dir - only scheduler/ and space/ subpaths symlink to brain - so ubi-lang.md is LOCAL at .../ratmac-skills/.arca/ubi-lang.md); #80 the gate shape (two-pass fail-fast: Pass 1 deterministic script prefilter -> Pass 2 three-lens subagent-team review; per-section advance-linter = fast script + advisory separation heuristics; team at the gate only, not per-advance). A read-only separation audit found REAL HOW-bleed in spec.md (graph/registry/message-mechanism detail at three sites) overlapping already-accepted-but-unexecuted cuts (dependency-graph migration to automata; message/IPC removal); remediation is deferred awaiting a user go-ahead, and one borderline (design.md scope duplicating spec.md ownership) took a provisional agent-decided call (ownership canonical in spec.md; design.md links). Conduct unchanged: a persistent artifact-writer with concise non-interrupting completion notices; questions in plain language with each premise cited or [assumption]-tagged and no bare code-names; a recommendation attached to every question; SPEC-affecting batches route through three review lenses to one consolidating executor. The scheduler DECISION.md is at 80 entries, all banked into the issue (i-ratmac-ratio); nothing is committed in git. Next: answer the active grill question - the lesson-store reuse boundary (per-project / per-skill-kind [main-agent lean] / global) - then grill the lesson-store schema; separation remediation, the RV simplification cuts, the handoff-contract thread, and a deferred adversarial-review pass remain pending.

## user-instructions
- Open replies with "Aye, m'lord"; when the LI baseline hook fires, open with "LI BASELINE ALIGNED:" plus a verbatim restatement of its rules.
- The main agent orchestrates only - every file/shell/MCP tool call goes to a subagent; a >20-unit job needs a Workflow with explicit opt-in.
- Knowledge .md files route ONLY to .arca/space/ratmac-sp/ (via brain/buf/ingest/ first); never into the scheduler tree, the store, or the project source tree.
- Contract-doc changes go through the design's own machinery: accepted decision -> DECISION.md entry -> reflected into spec/design.

## condensed-transcript
- U: Review the ratmac-ratio design against the ratmac-skills family vision plus obra/superpowers and aihero.dev grill-with-docs.
- A: Spawned readers: a repo investigation (shells, contract docs, review.md, README, family list) plus external-reference research.
- A: Review delivered: a keep-list (exact sign-off gates, lint-blocking sign-off, domain-labelled grilling, glossary), 7 fixes, and 3 user forks (post-publish source-of-truth, README truth, RV-4).
- U: Yes, apply your fixes.
- A: A builder applied all edits (shells, design.md, spec.md, DECISION.md, README, the SPACE.md rename); AGENTS.md, state.toml, and the open RVs were left untouched.
- U: Resolve the design debts by grilling me one question at a time; the main session now operates AS ratmac-ratio under the contract.
- A: Ran the grilling - decisions #60-#73 (escalation predicate #60, linter #61, RV hygiene #62-#66, role-collapse repair #67, lifecycle #68, writer model #69, framework adoption #70/#71, SEED intake gate #72, writer-fleet scrub #73); adversarial review waves converged and were consolidated.
- U: Now design ratio's OWN grilling flow.
- A: Banked the backbone as #75 - an artifactual spine (contract sections = coverage checkpoints, walked in dependency order, grill-toward = validate-against) plus a cross-session lesson store; drafted the per-turn SPEC loop, with DESIGN reusing it under one material-fork escalation predicate.
- U: Three corrections - the SEED IS the SPEC's original draft; DESIGN may edit cross-domain into spec.md; spec.md (WHAT) and design.md (HOW) stay strictly domain-separated, one cross-link only.
- A: Integrated the corrections (later banked as #76) and ran a read-only separation audit - found REAL HOW-bleed in spec.md (graph/registry/message-mechanism detail at three sites) overlapping already-accepted-but-unexecuted cuts (automata migration; message/IPC removal); remediation deferred pending a user go, with one borderline (design.md scope vs spec.md ownership) taken as a provisional agent call (ownership canonical in spec.md, design.md links).
- A: Banked #77 (amendment model - DESIGN-driven SPEC edits are scoped amendments, no per-edit re-sign; one full-scale reconciliation at DESIGN close re-ratifies the SPEC and hosts the three-lens adversarial review + consolidator) and #78 (advance = mechanical coverage only, fully frictionless; the per-turn loop finalized as orient -> self-answer -> locate unsatisfied section -> ask one recommended question -> integrate draft+glossary+decision-record -> advance-on-coverage -> close).
- A: Banked #79 (glossary = ONE project-wide .arca/ubi-lang.md governing scheduler + knowledge base, ratio writes/consumes but does not own, lazy-created, carve-out added to .arca/AGENTS.md; PATH FACT: .arca/ is a LOCAL repo dir, only scheduler/ and space/ subpaths symlink to brain, so ubi-lang.md is LOCAL) and #80 (sign-off/reconciliation gate = two-pass fail-fast: deterministic script prefilter -> three-lens subagent-team review; per-section advance-linter = fast script + advisory separation heuristics; team at the gate only). DECISION.md now at 80; all banking by the persistent artifact-writer into i-ratmac-ratio; nothing committed in git.
- A: Posed the current active grill question - the lesson-store reuse boundary (per-project / per-skill-kind [main-agent lean] / global); the lesson-store schema is queued next. Background auto-save written at this threshold.

## sources
- shells: skills/ratmac-ratio/SKILL.md, skills/ratmac-ratio/REFERENCE.md
- contract: .arca/scheduler/issue/i-ratmac-ratio/spec.md, design.md, DECISION.md (mount -> E:/repos/brain/scheduler/p-ratmac-skill/issue/i-ratmac-ratio/)
- audit: review.md (14 ranked RV over-engineering findings)
- family vision: README (R-invariants)
- knowledge space: brain/spaces/ratmac-sp/SPACE.md (renamed from SPACE.md.md)
- external: obra/superpowers skill-writing principles; Matt Pocock (aihero.dev) grill-with-docs technique

## insights
Keep-list (already strong, do not touch): exact sign-off gates; lint-gated sign-off; one-question grilling with domain labels; the linted glossary.

Seven fixes accepted and applied the same day:
1. SKILL.md description rewritten trigger-only (it was a leaked process summary).
2. Contract reading made a blocking MUST-gate; the turn skeleton marked not-executable-alone.
3. REFERENCE.md reduced to a stub (it duplicated contract content - completes the RV-2 de-mirror).
4. publish specified in design.md as deterministic, idempotent regeneration of GENERATED-fenced shells (R6/R10 aligned); the contract remains source of truth; the post-close durable home is recorded as an explicit open item.
5. Self-answer-before-asking grilling rule added to spec.md.
6. Sign-off gate armor added to design.md (plus one mirrored sentence in spec.md): paraphrase is not sign-off; no solicitation of the phrase; a lint failure refuses the phrase.
7. TEST-phase seed pressure scenarios recorded in design.md; an optional One-way marker added to the DECISION entry shape.

Deliberately left untouched: RV-4 (async writer-fleet + IRC serialization) and the AGENTS.md TEMP block; state.toml; the remaining open RVs (including RV-1's physical migration).

Housekeeping: brain/spaces/ratmac-sp/SPACE.md.md renamed to SPACE.md (zero references existed); README got a dated Delta note (ratio = 12th skill in DESIGN under the issue tier; R3 re-locks at publish; gen-2 names pdrft-brain-v3 pending).

## decisions
1. 2026-07-03 - The user accepted external-review fixes 1-7, including the GENERATED-shells recommendation ("Yes, apply your fixes"), and they were applied the same day by a builder subagent. RV-4 and the AGENTS.md TEMP block were deliberately left untouched. Recorded in the contract's DECISION.md as the batched entry titled "External review fixes". Do not edit this decision record later without an explicit user ask - contract-doc changes go through the design's own machinery (accepted decision -> DECISION.md entry -> reflected into spec/design).
